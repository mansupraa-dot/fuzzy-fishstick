# Plan 5: Home — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rebuild HomePage and its sections to Light Glass — same content, new visual layer matching all inner pages.

**Architecture:** Four isolated file changes (HowWeWork, Portfolio, CTA, HomePage) plus four new test files. Each section rebuild is independent. HomePage is last as it ties the others together. Test count goes 84 → 92.

**Tech Stack:** React 19 + Vite + Tailwind v4 CSS-first + Vitest + @testing-library/react (jsdom, globals: true, pool: forks). Run tests: `npm run test:run`.

---

## File Map

| Action | File |
|--------|------|
| Modify | `src/sections/HowWeWork.jsx` |
| Create | `src/sections/__tests__/HowWeWork.test.jsx` |
| Modify | `src/sections/Portfolio.jsx` |
| Create | `src/sections/__tests__/Portfolio.test.jsx` |
| Modify | `src/sections/CTA.jsx` |
| Create | `src/sections/__tests__/CTA.test.jsx` |
| Modify | `src/pages/HomePage.jsx` |
| Create | `src/pages/__tests__/HomePage.test.jsx` |

---

### Task 1: HowWeWork — Light Glass rebuild

**Files:**
- Modify: `src/sections/HowWeWork.jsx`
- Create: `src/sections/__tests__/HowWeWork.test.jsx`

Current state: uses `bg-cream`, `text-graphite`, `text-accent`, heading «Три шага». Needs: plain `#EFEFEF` bg (no bg class), `text-2xl font-light text-ink` heading «Как это работает», step numbers `text-6xl font-light text-ink/8`, `text-xl font-light text-ink` titles, `text-sm text-ink-3` descriptions, `grid md:grid-cols-3 gap-10` layout.

- [ ] **Step 1: Write the failing tests**

Create `src/sections/__tests__/HowWeWork.test.jsx`:

```jsx
import { render, screen } from '@testing-library/react'
import HowWeWork from '../HowWeWork'

describe('HowWeWork', () => {
  it('renders heading «Как это работает»', () => {
    render(<HowWeWork />)
    expect(screen.getByText('Как это работает')).toBeInTheDocument()
  })

  it('renders step numbers 01, 02, 03', () => {
    render(<HowWeWork />)
    expect(screen.getByText('01')).toBeInTheDocument()
    expect(screen.getByText('02')).toBeInTheDocument()
    expect(screen.getByText('03')).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `npm run test:run -- src/sections/__tests__/HowWeWork.test.jsx`

Expected: FAIL — heading «Три шага» ≠ «Как это работает»

- [ ] **Step 3: Rewrite HowWeWork.jsx**

Replace the entire file content:

```jsx
import Reveal from '../components/ui/Reveal'

const STEPS = [
  {
    number: '01',
    title: 'Разговор',
    description:
      'Рассказываете о квартире, задаче, бюджете. Мы слушаем — без шаблонов.',
  },
  {
    number: '02',
    title: 'Подбор',
    description:
      'Архитектор анализирует пространство и подбирает конкретные позиции из каталога.',
  },
  {
    number: '03',
    title: 'Результат',
    description:
      'Получаете точный список: что, откуда, почём. Без лишних согласований.',
  },
]

export default function HowWeWork() {
  return (
    <section className="py-24 md:py-32">
      <div className="max-w-6xl mx-auto px-6">
        <Reveal>
          <h2 className="text-2xl font-light text-ink mb-16">
            Как это работает
          </h2>
        </Reveal>
        <div className="grid md:grid-cols-3 gap-10">
          {STEPS.map((step) => (
            <Reveal key={step.number}>
              <div>
                <div className="text-6xl font-light text-ink/8 select-none mb-4">
                  {step.number}
                </div>
                <h3 className="text-xl font-light text-ink mb-3">
                  {step.title}
                </h3>
                <p className="text-sm text-ink-3 leading-relaxed">
                  {step.description}
                </p>
              </div>
            </Reveal>
          ))}
        </div>
      </div>
    </section>
  )
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `npm run test:run -- src/sections/__tests__/HowWeWork.test.jsx`

Expected: PASS — 2/2

- [ ] **Step 5: Run full suite to check for regressions**

Run: `npm run test:run`

Expected: 86/86 PASS (84 existing + 2 new)

- [ ] **Step 6: Commit**

```bash
git add src/sections/HowWeWork.jsx src/sections/__tests__/HowWeWork.test.jsx
git commit -m "feat: rebuild HowWeWork to Light Glass"
```

---

### Task 2: Portfolio section — import from data, status badges

**Files:**
- Modify: `src/sections/Portfolio.jsx`
- Create: `src/sections/__tests__/Portfolio.test.jsx`

Current state: inline `PROJECTS` array (4 items, no slug/status), `bg-cream`, `bg-stone-300`, `text-graphite`, `text-accent`, `grid md:grid-cols-2`. Needs: import from `src/data/portfolio.js`, `slice(0, 3)`, `glass-card`, `grid md:grid-cols-3 gap-4`, status badges, Link to `/portfolio/${p.slug}`, CTA link → `/portfolio`.

- [ ] **Step 1: Write the failing tests**

Create `src/sections/__tests__/Portfolio.test.jsx`:

```jsx
import { render, screen } from '@testing-library/react'
import { MemoryRouter } from 'react-router-dom'
import Portfolio from '../Portfolio'

describe('Portfolio section', () => {
  it('renders heading «Портфолио»', () => {
    render(
      <MemoryRouter>
        <Portfolio />
      </MemoryRouter>
    )
    expect(screen.getByText('Портфолио')).toBeInTheDocument()
  })

  it('shows at least one «Реализован» badge', () => {
    render(
      <MemoryRouter>
        <Portfolio />
      </MemoryRouter>
    )
    expect(screen.getAllByText('Реализован').length).toBeGreaterThan(0)
  })
})
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `npm run test:run -- src/sections/__tests__/Portfolio.test.jsx`

Expected: FAIL — heading «Портфолио» not found (current heading says «Портфолио»... actually check: current file may use different heading or import path will fail on inline PROJECTS vs data import)

- [ ] **Step 3: Rewrite Portfolio.jsx**

Replace the entire file content:

```jsx
import { Link } from 'react-router-dom'
import Reveal from '../components/ui/Reveal'
import { PROJECTS } from '../data/portfolio'

const STATUS_LABELS = {
  completed: 'Реализован',
  in_progress: 'В реализации',
  concept: 'Концепт',
}

const STATUS_STYLES = {
  completed: 'bg-emerald-50 text-emerald-700 border border-emerald-200',
  in_progress: 'bg-amber-50 text-amber-700 border border-amber-200',
  concept: 'bg-black/5 text-ink-3 border border-black/10',
}

export default function Portfolio() {
  const preview = PROJECTS.slice(0, 3)

  return (
    <section className="py-24 md:py-32">
      <div className="max-w-6xl mx-auto px-6">
        <Reveal>
          <h2 className="text-2xl font-light text-ink mb-16">Портфолио</h2>
        </Reveal>
        <div className="grid md:grid-cols-3 gap-4 mb-10">
          {preview.map((p) => (
            <Reveal key={p.id}>
              <Link to={`/portfolio/${p.slug}`} className="glass-card overflow-hidden block group">
                <div className="aspect-[4/3] bg-white/40" />
                <div className="p-4">
                  <p className="text-[13px] font-normal text-ink mb-2">
                    {p.title}
                  </p>
                  <span
                    className={`inline-block text-[11px] px-2 py-0.5 rounded-full ${STATUS_STYLES[p.status]}`}
                  >
                    {STATUS_LABELS[p.status]}
                  </span>
                  <p className="text-xs text-ink-4 mt-2">
                    {p.area} · {p.tags.join(', ')}
                  </p>
                </div>
              </Link>
            </Reveal>
          ))}
        </div>
        <Reveal>
          <Link
            to="/portfolio"
            className="text-sm text-ink border-b border-ink/30 hover:border-ink transition-colors"
          >
            Смотреть все проекты
          </Link>
        </Reveal>
      </div>
    </section>
  )
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `npm run test:run -- src/sections/__tests__/Portfolio.test.jsx`

Expected: PASS — 2/2

- [ ] **Step 5: Run full suite to check for regressions**

Run: `npm run test:run`

Expected: 88/88 PASS (86 + 2 new)

- [ ] **Step 6: Commit**

```bash
git add src/sections/Portfolio.jsx src/sections/__tests__/Portfolio.test.jsx
git commit -m "feat: rebuild Portfolio section — import from data, status badges"
```

---

### Task 3: CTA — Light Glass rebuild with Link to /brief

**Files:**
- Modify: `src/sections/CTA.jsx`
- Create: `src/sections/__tests__/CTA.test.jsx`

Current state: `bg-white`, `text-graphite`, `text-stone-*`, no `Link` import, 2-column layout with inline form and Telegram/WhatsApp links. Needs: single `glass-card p-10 md:p-16 text-center`, heading «Расскажите о проекте», subtext, `<Link to="/brief">` button styled `bg-ink text-white text-sm px-8 py-3 rounded-full hover:bg-ink/85`.

- [ ] **Step 1: Write the failing tests**

Create `src/sections/__tests__/CTA.test.jsx`:

```jsx
import { render, screen } from '@testing-library/react'
import { MemoryRouter } from 'react-router-dom'
import CTA from '../CTA'

describe('CTA section', () => {
  it('renders heading «Расскажите о проекте»', () => {
    render(
      <MemoryRouter>
        <CTA />
      </MemoryRouter>
    )
    expect(screen.getByText('Расскажите о проекте')).toBeInTheDocument()
  })

  it('CTA link points to /brief', () => {
    render(
      <MemoryRouter>
        <CTA />
      </MemoryRouter>
    )
    const link = screen.getByRole('link', { name: /оставить заявку/i })
    expect(link).toHaveAttribute('href', '/brief')
  })
})
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `npm run test:run -- src/sections/__tests__/CTA.test.jsx`

Expected: FAIL — heading not found (current says different text), no Link to /brief

- [ ] **Step 3: Rewrite CTA.jsx**

Replace the entire file content:

```jsx
import { Link } from 'react-router-dom'
import Reveal from '../components/ui/Reveal'

export default function CTA() {
  return (
    <section className="py-24 md:py-32">
      <div className="max-w-3xl mx-auto px-6">
        <Reveal>
          <div className="glass-card p-10 md:p-16 text-center">
            <h2 className="text-2xl font-light text-ink mb-4">
              Расскажите о проекте
            </h2>
            <p className="text-sm text-ink-3 mb-8">
              Оставьте контакт — свяжемся в течение дня.
            </p>
            <Link
              to="/brief"
              className="inline-block bg-ink text-white text-sm px-8 py-3 rounded-full hover:bg-ink/85 transition-colors"
            >
              Оставить заявку
            </Link>
          </div>
        </Reveal>
      </div>
    </section>
  )
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `npm run test:run -- src/sections/__tests__/CTA.test.jsx`

Expected: PASS — 2/2

- [ ] **Step 5: Run full suite to check for regressions**

Run: `npm run test:run`

Expected: 90/90 PASS (88 + 2 new)

- [ ] **Step 6: Commit**

```bash
git add src/sections/CTA.jsx src/sections/__tests__/CTA.test.jsx
git commit -m "feat: rebuild CTA section to Light Glass with Link to /brief"
```

---

### Task 4: HomePage — remove Philosophy/popular products, rebuild inline categories, add HowWeWork

**Files:**
- Modify: `src/pages/HomePage.jsx`
- Create: `src/pages/__tests__/HomePage.test.jsx`

Current state: imports `Philosophy`, `getPopularProducts`, `ProductCard`, `Reveal`; sections: Hero + Categories (old tokens) + Popular Products (bg-cream) + Philosophy + CTA. Needs: remove Philosophy + popular products imports and sections; rebuild inline categories as `glass-card` tiles in `grid md:grid-cols-3 gap-6`; add `HowWeWork` import + `<HowWeWork />` between categories and Portfolio.

Section order:
```
<Hero />
<section py-20> categories inline glass-card grid </section>
<HowWeWork />
<Portfolio />
<CTA />
```

- [ ] **Step 1: Write the failing tests**

Create `src/pages/__tests__/HomePage.test.jsx`:

```jsx
import { render, screen } from '@testing-library/react'
import { MemoryRouter } from 'react-router-dom'
import { HelmetProvider } from 'react-helmet-async'
import HomePage from '../HomePage'

const Wrapper = ({ children }) => (
  <HelmetProvider>
    <MemoryRouter>{children}</MemoryRouter>
  </HelmetProvider>
)

describe('HomePage', () => {
  it('renders without crash', () => {
    render(<HomePage />, { wrapper: Wrapper })
    expect(screen.getByText('Каталог')).toBeInTheDocument()
  })

  it('category links point to /catalog/furniture, /catalog/lighting, /catalog/plumbing', () => {
    render(<HomePage />, { wrapper: Wrapper })
    expect(screen.getByRole('link', { name: /мебель/i })).toHaveAttribute(
      'href',
      '/catalog/furniture'
    )
    expect(screen.getByRole('link', { name: /освещение/i })).toHaveAttribute(
      'href',
      '/catalog/lighting'
    )
    expect(screen.getByRole('link', { name: /сантехника/i })).toHaveAttribute(
      'href',
      '/catalog/plumbing'
    )
  })
})
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `npm run test:run -- src/pages/__tests__/HomePage.test.jsx`

Expected: FAIL — links not found with /catalog/furniture etc. (current may have different hrefs or old tokens)

- [ ] **Step 3: Read current HomePage.jsx to identify all imports and sections to keep/remove**

Read `src/pages/HomePage.jsx` and identify:
- Which imports to keep: `Hero`, `Portfolio`, `CTA`, `Link` (from react-router-dom), `Helmet`/`HelmetProvider`
- Which to remove: `Philosophy`, `getPopularProducts`, `ProductCard`, `Reveal` (if unused after rebuild)
- Which to add: `HowWeWork` from `../sections/HowWeWork`

- [ ] **Step 4: Rewrite HomePage.jsx**

Replace the entire file content (adapt Helmet title/meta from whatever is currently there — do not change them):

```jsx
import { Helmet } from 'react-helmet-async'
import { Link } from 'react-router-dom'
import Hero from '../sections/Hero'
import HowWeWork from '../sections/HowWeWork'
import Portfolio from '../sections/Portfolio'
import CTA from '../sections/CTA'

const CATEGORIES = [
  {
    label: 'Мебель',
    description: 'Диваны, кресла, столы, стеллажи от европейских производителей.',
    href: '/catalog/furniture',
  },
  {
    label: 'Освещение',
    description: 'Люстры, бра, трековые системы — свет как элемент интерьера.',
    href: '/catalog/lighting',
  },
  {
    label: 'Сантехника',
    description: 'Раковины, смесители, душевые системы премиум-класса.',
    href: '/catalog/plumbing',
  },
]

export default function HomePage() {
  return (
    <>
      <Helmet>
        <title>Pufflux — премиум мебель, свет и сантехника</title>
        <meta
          name="description"
          content="Премиум интерьер из одного места — мебель, освещение и сантехника от лучших брендов."
        />
      </Helmet>

      <Hero />

      <section className="py-20">
        <div className="max-w-6xl mx-auto px-6">
          <h2 className="text-2xl font-light text-ink mb-10">Каталог</h2>
          <div className="grid md:grid-cols-3 gap-6">
            {CATEGORIES.map((cat) => (
              <Link
                key={cat.href}
                to={cat.href}
                className="glass-card overflow-hidden block group"
              >
                <div className="aspect-[4/3] bg-white/40" />
                <div className="p-5">
                  <p className="text-base font-light text-ink mb-1">
                    {cat.label}
                  </p>
                  <p className="text-sm text-ink-3 mb-3">{cat.description}</p>
                  <span className="text-ink-4 group-hover:text-ink transition-colors text-sm">
                    →
                  </span>
                </div>
              </Link>
            ))}
          </div>
        </div>
      </section>

      <HowWeWork />
      <Portfolio />
      <CTA />
    </>
  )
}
```

**Note:** If the current HomePage has a different Helmet title/description, keep whatever is there — don't change it unless it references old branding that needs correcting. The above is a safe default if it's a stub.

- [ ] **Step 5: Run tests to verify they pass**

Run: `npm run test:run -- src/pages/__tests__/HomePage.test.jsx`

Expected: PASS — 2/2

- [ ] **Step 6: Run full suite**

Run: `npm run test:run`

Expected: 92/92 PASS (90 + 2 new)

- [ ] **Step 7: Commit**

```bash
git add src/pages/HomePage.jsx src/pages/__tests__/HomePage.test.jsx
git commit -m "feat: rebuild HomePage — Light Glass categories, add HowWeWork, remove Philosophy and popular products"
```

---

## Self-Review

**Spec coverage check:**

| Spec requirement | Covered in |
|---|---|
| Hero — no change | Spec says no change; HomePage still imports `<Hero />` |
| Categories — glass-card tiles, grid-cols-3, text-ink tokens | Task 4 |
| HowWeWork — text-ink tokens, heading «Как это работает», step numbers | Task 1 |
| Portfolio — import PROJECTS, slice(0,3), status badges, grid-cols-3 | Task 2 |
| CTA — glass-card, Link to /brief, remove Telegram | Task 3 |
| HomePage — remove Philosophy + popular products, add HowWeWork import | Task 4 |
| 8 new tests (84 → 92) | All 4 tasks (2 tests each) |

**Placeholder scan:** No TBD, no "implement later", all code blocks are complete.

**Type consistency:** `PROJECTS` export from `src/data/portfolio.js` has `slug`, `status`, `area`, `tags` — all referenced in Task 2. `STATUS_LABELS`/`STATUS_STYLES` keys match status values in data (`completed`, `in_progress`, `concept`). `Link` imported in CTA (added in Task 3) and Portfolio (already present). HomePage imports match section names defined in Tasks 1–3.
